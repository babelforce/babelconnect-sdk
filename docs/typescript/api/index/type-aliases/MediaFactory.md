# Type Alias: MediaFactory()

```ts
type MediaFactory = (callId) => Media;
```

Builds the [Media](../interfaces/Media.md) for a given call id.

## Parameters

### callId

`string`

## Returns

[`Media`](../interfaces/Media.md)
